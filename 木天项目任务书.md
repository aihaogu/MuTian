# 木天 macOS App 项目任务书

版本：0.1  
日期：2026-04-29  
项目名称：木天
目标平台：macOS  
当前依赖：本目录下 `bookget` 项目作为古籍下载引擎

## 1. 项目背景

古籍数字资源分散在国家图书馆、各地古籍平台、日本、欧美图书馆、IIIF 站点以及个人本地文件夹中。用户在实际使用时通常会遇到几个问题：

- 下载入口分散，下载后的文件夹结构不统一。
- 本地古籍数量增加后，缺少统一的收藏、分类、统计和搜索工具。
- 古籍分类需要贴近传统目录学，不能只按普通文件夹或标签管理。
- 阅读、校对、版本比较、OCR 识别等工作往往和收藏管理割裂。

“木天”拟做成一个本地优先的 macOS 古籍资料库应用。第一阶段重点不是在线平台，而是把用户已经下载或正在下载的古籍整理成一个可收藏、可分类、可检索、可统计、可阅读的本地知识库；下载功能复用现有 `bookget` 项目；后续再扩展智能识别与校书能力。

## 2. 项目定位

木天是面向古籍收藏、整理和阅读的 macOS 桌面应用。

核心定位：

- 本地古籍资料库：读取本地文件夹中的古籍影像、PDF、文本、元数据文件，并建立索引。
- 四库体例分类：以经、史、子、集四部为核心分类体系，支持细分类目和用户自定义补充。
- 收藏和统计：把古籍当作“藏书条目”管理，而不是只管理文件。
- 搜索入口：支持书名、作者、版本、朝代、馆藏来源、分类、标签、全文或 OCR 文本搜索。
- 下载入口：通过 `bookget` 下载数字古籍，并将下载结果导入资料库。
- 阅读和校对基础：先实现稳定阅读；后续加入 OCR、智能识别、自动校对、人工校对和版本比对。

## 3. 当前项目基础

当前目录已有 `bookget` 项目：

- `bookget` 是 Go 项目，模块名为 `bookget`，当前 `go.mod` 使用 Go 1.23。
- README 标明其为“数字古籍图书下载工具”，支持约 50+ 个数字图书馆。
- 命令行参数已覆盖下载 URL、输出目录、页码范围、册数范围、IIIF 格式、线程数、并发数、重试次数、输出格式、Cookie、Header、User-Agent 等。
- `router/interface.go` 中已按站点域名注册多个古籍平台和 IIIF 下载路由。
- `bookget-gui` 目录是 Windows WebView2 GUI，不作为 macOS 主界面基础。

木天第一阶段不重写 `bookget` 下载逻辑，而是将其作为可执行下载引擎调用，并在 macOS App 中提供任务创建、参数配置、进度展示、结果导入和错误记录。

## 4. 用户对象

主要用户：

- 古籍收藏和整理者：需要把分散下载的古籍归档、分类和检索。
- 文史研究者：需要快速查找同书异本、版本、作者、馆藏来源和影像页。
- 校勘/整理工作者：后续需要 OCR、校对、批注、版本对读和校记管理。
- 学习型用户：希望按四库体例浏览古籍，形成自己的古籍资料库。

## 5. 一期目标

一期目标是构建一个可用的本地古籍收藏管理器，重点完成“导入、分类、收藏、统计、搜索、阅读、下载导入”。

必须完成：

- 创建 macOS 原生 App：应用名称为“木天”。
- 建立本地资料库：保存书目、分类、标签、文件路径、阅读状态、下载任务等数据。
- 扫描本地文件夹：识别古籍目录、PDF、图片序列、文本/OCR 文件和元数据文件。
- 四库分类树：提供经、史、子、集四部及常见细目，支持未分类和自定义分类。
- 书目管理：支持收藏、取消收藏、编辑书名/作者/版本/朝代/来源/备注。
- 统计面板：按四部、细类、来源、文件类型、下载状态、收藏状态统计。
- 搜索功能：支持快速搜索书名、作者、标签、来源、备注；预留全文索引入口。
- 阅读功能：支持 PDF 和图片序列的基础阅读。
- 下载功能：在 App 中创建 `bookget` 下载任务，下载完成后导入资料库。
- 设置功能：配置资料库路径、下载目录、`bookget` 可执行文件路径、默认并发/重试/格式参数。

暂不强制完成：

- 云同步。
- 用户账号。
- 在线书目数据库。
- 完整 OCR 和自动校勘。
- 多人协作。

## 6. 功能范围

### 6.1 资料库与导入

本地资料库应支持多个根目录。每个根目录可以是用户已有古籍文件夹，也可以是 `bookget` 下载目录。

导入方式：

- 选择文件夹导入。
- 拖拽文件夹或文件导入。
- 扫描指定根目录并增量更新。
- 下载任务完成后自动导入。

识别对象：

- 图片序列：`.jpg`、`.jpeg`、`.png`、`.tif`、`.tiff`、`.webp`。
- 文档：`.pdf`。
- 文本：`.txt`、`.md`、`.xml`、`.json`。
- 元数据：同目录下的 `metadata.json`、`manifest.json`、`info.json`、`urls.txt`、下载日志等。

导入规则：

- 一个古籍条目可以对应一个 PDF，也可以对应一个包含多页影像的文件夹。
- 默认使用文件夹名或文件名作为书名候选。
- 若存在 IIIF manifest 或元数据文件，优先解析题名、作者、馆藏来源、页数等字段。
- 无法识别的信息进入“待整理”状态，不阻塞导入。
- 保存原始文件路径，不强制移动用户文件。

### 6.2 收藏与书目管理

每个古籍条目至少包含：

- 书名。
- 作者。
- 朝代。
- 版本/刊刻信息。
- 四库分类。
- 标签。
- 来源平台或馆藏机构。
- 本地路径。
- 文件类型。
- 页数。
- 收藏状态。
- 阅读进度。
- 备注。
- 导入时间和更新时间。

收藏功能：

- 一键收藏/取消收藏。
- 收藏列表。
- 按分类、标签、来源筛选收藏。
- 收藏状态不改变文件位置。

编辑功能：

- 支持手动修改书目信息。
- 支持批量设置分类、标签、来源。
- 支持“待整理”“已整理”“需校对”等状态。

### 6.3 四库分类体系

默认分类树采用四部体例：

- 经部：易、书、诗、礼、春秋、孝经、五经总义、四书、乐、小学。
- 史部：正史、编年、纪事本末、别史、杂史、诏令奏议、传记、史钞、载记、时令、地理、职官、政书、目录、史评。
- 子部：儒家、兵家、法家、农家、医家、天文算法、术数、艺术、谱录、杂家、类书、小说家、释家、道家。
- 集部：楚辞、别集、总集、诗文评、词曲。
- 其他：丛书、方志、谱牒、敦煌吐鲁番文献、未分类、自定义。

分类要求：

- 四部分类是主分类，不替代标签。
- 每本书只允许一个主分类，但可以有多个标签。
- 支持用户新增、重命名、隐藏细类。
- 统计优先按四部和细类聚合。
- 搜索结果中应显示分类路径，例如“史部 / 地理 / 方志”。

### 6.4 搜索与筛选

一期搜索分两层：

基础搜索：

- 书名。
- 作者。
- 朝代。
- 版本。
- 标签。
- 来源。
- 备注。
- 本地路径。

高级筛选：

- 四库分类。
- 文件类型。
- 是否收藏。
- 是否已整理。
- 是否有 OCR 文本。
- 是否由 `bookget` 下载。
- 导入时间范围。
- 页数范围。

后续全文搜索：

- OCR 文本入库后，使用 SQLite FTS5 或同类全文索引。
- 支持繁简、异体字、通假字扩展匹配时，应作为独立阶段处理。

### 6.5 阅读功能

一期阅读目标是稳定、快速、够用。

PDF 阅读：

- 打开 PDF。
- 页码跳转。
- 缩放。
- 连续滚动。
- 记录阅读进度。

图片序列阅读：

- 按文件名自然排序。
- 上一页/下一页。
- 缩放、适宽、适高。
- 缩略图页栏。
- 记录当前页。

阅读辅助：

- 显示书目信息。
- 显示本地路径。
- 支持在 Finder 中显示。
- 支持添加页级备注，作为后续校书功能基础。

### 6.6 下载功能

下载功能由 `bookget` 实现，木天负责界面、任务管理和导入。

App 中的下载任务字段：

- URL。
- 下载目录。
- 页码范围。
- 册数范围。
- 下载模式：默认、通用批量、IIIF manifest。
- 图片格式或 IIIF format。
- 文件扩展名。
- 线程数。
- 并发数。
- 重试次数。
- 超时时间。
- Sleep 间隔。
- Cookie 文件。
- Header 文件。
- User-Agent。

集成方式：

- 将 `bookget` 编译为 macOS 可执行文件。
- App 通过 `Process` 启动 `bookget` 子进程。
- 捕获 stdout/stderr，用于展示日志和状态。
- 下载任务写入本地任务表。
- 下载完成后扫描输出目录并创建古籍条目。

下载状态：

- 待开始。
- 运行中。
- 已暂停。
- 已完成。
- 失败。
- 已取消。
- 已导入。

一期可以先不实现真正的断点续传暂停，只需要支持取消进程和失败后重试；若 `bookget` 本身支持续传，再在二期增强。

### 6.7 统计功能

统计面板应回答几个常见问题：

- 当前资料库共有多少部古籍。
- 收藏了多少部。
- 各四部和细类分别有多少部。
- 各来源平台分别有多少部。
- PDF、图片序列、文本类分别有多少。
- 有多少条目未分类。
- 有多少条目缺作者、缺朝代、缺版本信息。
- 近期导入和近期下载数量。

统计结果需要支持点击跳转到对应筛选列表。

### 6.8 后续校书功能规划

校书功能作为二期或三期展开，一期只预留数据结构和入口。

目标能力：

- OCR：对古籍影像进行文字识别，生成页级文本。
- 智能识别：识别书名、卷次、版心、页码、藏书印、序跋、目录等。
- 自动校对：将 OCR 文本与底本、通行本或其他版本文本比对。
- 人工校对：提供影像与文本对照界面，支持逐字改校。
- 校记管理：记录异文、衍文、脱文、倒文、误字等。
- 版本对读：同一书多版本并列阅读和差异定位。

一期预留：

- 每页可绑定 OCR 文本。
- 每页可绑定人工校对文本。
- 每页可保存备注。
- 每本书可标记“需校对”“校对中”“已校对”。

## 7. 推荐技术方案

### 7.1 App 技术栈

推荐：

- SwiftUI：主界面、列表、详情、设置、工具栏。
- AppKit 小桥接：文件打开面板、Finder 定位、PDF/图像阅读中 SwiftUI 不足的部分。
- SQLite：核心资料库。
- SQLite FTS5：全文搜索和书目搜索。
- Swift Concurrency：扫描、索引、下载任务调度。
- Go `bookget` 可执行文件：下载引擎。

不建议一期直接使用重型服务端架构。木天应先做成本地原生应用，数据可迁移、文件路径透明、离线可用。

### 7.2 macOS 窗口结构

主窗口使用三栏布局：

- 左侧边栏：资料库、收藏、四库分类、标签、来源、下载、待整理。
- 中间列表：古籍条目列表，支持搜索、排序和筛选。
- 右侧详情/阅读区：书目信息、阅读器、统计详情或下载任务详情。

独立窗口：

- 设置窗口。
- 下载任务窗口或下载详情面板。
- 后续校对窗口。

主界面应符合 macOS 桌面习惯：

- 使用原生侧边栏。
- 顶部工具栏放置导入、下载、新建分类、搜索、视图切换等常用操作。
- 菜单栏提供导入、打开资料库、导出书目、刷新索引等命令。
- 常用操作支持键盘快捷键。

### 7.3 建议目录结构

后续构建工程时建议采用：

```text
木天/
  MuTian/
    App/
      MuTianApp.swift
    Views/
      ContentView.swift
      SidebarView.swift
      LibraryListView.swift
      BookDetailView.swift
      ReaderView.swift
      DownloadView.swift
      StatisticsView.swift
      SettingsView.swift
    Models/
      Book.swift
      Classification.swift
      Tag.swift
      LibraryRoot.swift
      DownloadTask.swift
      PageRecord.swift
    Stores/
      LibraryStore.swift
      SearchStore.swift
      SettingsStore.swift
      DownloadTaskStore.swift
    Services/
      FolderScanner.swift
      MetadataParser.swift
      BookgetClient.swift
      ThumbnailService.swift
      ReaderService.swift
    Persistence/
      Database.swift
      Migrations/
    Support/
      NaturalSort.swift
      FileTypeDetector.swift
      PathResolver.swift
  bookget/
  scripts/
    build_bookget_macos.sh
    build_and_run.sh
  docs/
    木天项目任务书.md
```

当前任务书先放在项目根目录。正式建工程时，可以移动到 `docs/`。

## 8. 数据模型草案

### 8.1 Book

代表一部古籍。

字段建议：

- `id`
- `title`
- `normalizedTitle`
- `author`
- `dynasty`
- `edition`
- `sourceName`
- `sourceURL`
- `localPath`
- `fileType`
- `pageCount`
- `classificationID`
- `isFavorite`
- `status`
- `notes`
- `createdAt`
- `updatedAt`
- `lastReadPage`
- `lastReadAt`

### 8.2 Classification

代表四库分类。

字段建议：

- `id`
- `parentID`
- `name`
- `level`
- `sortOrder`
- `isSystem`
- `isHidden`

### 8.3 Tag

代表用户标签。

字段建议：

- `id`
- `name`
- `color`
- `createdAt`

### 8.4 PageRecord

代表页级信息，为阅读和校书预留。

字段建议：

- `id`
- `bookID`
- `pageIndex`
- `imagePath`
- `ocrText`
- `correctedText`
- `notes`
- `status`

### 8.5 DownloadTask

代表一次 `bookget` 下载任务。

字段建议：

- `id`
- `url`
- `outputDirectory`
- `mode`
- `sequenceRange`
- `volumeRange`
- `format`
- `fileExt`
- `threads`
- `concurrent`
- `retries`
- `timeout`
- `sleep`
- `status`
- `stdoutLog`
- `stderrLog`
- `createdAt`
- `startedAt`
- `finishedAt`
- `importedBookID`

## 9. 一期里程碑

### M1：项目骨架与资料库

交付：

- 创建 macOS App 工程。
- 建立基础窗口、侧边栏和设置页。
- 建立 SQLite 数据库和迁移机制。
- 内置四库分类初始数据。

验收：

- App 可启动。
- 可打开主窗口和设置窗口。
- 数据库可创建、迁移、读写。
- 四库分类能在侧边栏显示。

### M2：本地扫描与导入

交付：

- 文件夹选择和导入。
- PDF、图片序列识别。
- 基础元数据解析。
- 导入结果列表。

验收：

- 选择一个本地古籍文件夹后能生成书目条目。
- 图片序列页数准确。
- PDF 可识别为单本书。
- 重复导入不会重复创建同一路径条目。

### M3：收藏、分类、统计

交付：

- 收藏和取消收藏。
- 编辑书目信息。
- 设置四库分类和标签。
- 统计面板。

验收：

- 分类修改后统计实时更新。
- 收藏列表只显示收藏条目。
- 未分类条目可单独筛选。

### M4：搜索与筛选

交付：

- 书目字段搜索。
- 分类、标签、来源、状态筛选。
- 排序。

验收：

- 搜索书名、作者、来源可返回正确条目。
- 筛选条件组合后结果正确。
- 大量条目下仍能快速响应。

### M5：阅读器

交付：

- PDF 阅读。
- 图片序列阅读。
- 页码跳转和阅读进度。
- Finder 定位。

验收：

- 打开书目后可阅读。
- 关闭重开后能恢复阅读进度。
- 图片序列顺序正确。

### M6：bookget 下载集成

交付：

- 编译 macOS `bookget` 可执行文件。
- App 中创建下载任务。
- 调用 `bookget` 子进程。
- 展示日志和状态。
- 下载完成后导入。

验收：

- 输入支持站点 URL 后可启动下载。
- 可配置输出目录、页码、格式、线程/并发等核心参数。
- 下载结束后 App 能扫描结果并创建书目。
- 失败任务有错误日志。

## 10. 验收标准

一期完成时，木天应达到：

- 用户可以把本地古籍文件夹导入木天。
- 用户可以按四库体例管理和统计古籍。
- 用户可以收藏、编辑、搜索和筛选古籍。
- 用户可以阅读 PDF 或图片序列。
- 用户可以通过木天调用 `bookget` 下载古籍，并把下载结果导入资料库。
- 应用数据保存在本地，退出重启后不丢失。
- 不破坏用户原始文件，不擅自移动或重命名古籍文件。

## 11. 非功能要求

性能：

- 资料库 1 万条书目内，基础搜索应接近即时响应。
- 文件夹扫描应异步执行，不阻塞主界面。
- 缩略图生成应缓存。

可靠性：

- 扫描失败、解析失败、下载失败都应记录错误，不导致 App 崩溃。
- 数据库迁移需要可重复执行。
- 下载任务日志需要可查看。

隐私：

- 默认本地保存数据。
- 不上传用户文件和书目信息。
- 若后续接入 AI/OCR 云服务，必须显式提示并提供本地/云端开关。

可维护性：

- `bookget` 与 App 主工程保持边界清晰。
- 下载集成通过 `BookgetClient` 统一封装。
- 分类、扫描、搜索、阅读、下载分别拆分模块。

版权与合规：

- 下载功能应尊重各古籍平台条款和访问限制。
- App 不绕过权限和付费限制。
- 对需要登录、Cookie 或真人验证的站点，只提供用户自备凭据和本地调用能力。

## 12. 风险与待确认问题

风险：

- 各古籍平台页面和接口变化会影响 `bookget` 下载成功率。
- 下载后的目录结构不统一，导入规则需要逐步适配。
- 古籍书名、卷次、版本信息常常不规范，自动识别不能完全替代人工整理。
- OCR 和校勘功能技术复杂，应避免在一期拖累核心资料库。
- macOS 沙盒权限可能影响访问任意本地文件夹，需设计安全书签或明确采用非沙盒分发。

待确认：

- App 是否需要进入 Mac App Store。如果需要，`bookget` 子进程、文件访问和下载能力都要重新评估沙盒限制。
- 一期是否只支持本机单用户，还是需要预留同步。
- 默认资料库路径放在 `~/Library/Application Support/木天/`，还是放在用户指定目录。
- 下载结果是否允许 App 自动整理到统一目录，还是始终保留 `bookget` 原始输出。
- 四库分类是否要严格采用《四库全书总目》细目，还是采用“实用四库体例 + 自定义”。

## 13. 二期与三期方向

二期：

- OCR 文本导入和全文检索。
- 页级备注和人工校对界面。
- 书目批量清洗。
- 同书异本关联。
- 导出书目 CSV/JSON。

三期：

- 智能识别书名、卷次、页码、版心。
- AI 辅助校对。
- 多版本对读和自动差异比对。
- 校记导出。
- 个人古籍研究专题库。

## 14. 第一阶段构建建议

下一步不应直接做完整大功能，而应先构建最小可用闭环：

1. 建立 macOS App 工程和 SQLite 数据库。
2. 内置四库分类树。
3. 实现本地文件夹导入，识别 PDF 和图片序列。
4. 实现书目列表、详情、收藏、分类。
5. 实现基础搜索。
6. 再接入 `bookget` 下载任务。

这样可以先验证“木天”的核心价值：把古籍从散乱文件变成可收藏、可分类、可搜索、可阅读的本地资料库。
